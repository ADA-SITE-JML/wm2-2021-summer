# Enabling H2 Console
spring.h2.console.enabled=true
# Connection data
spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.username=jamal
spring.datasource.password=wm2passForDB
spring.jpa.defer-datasource-initialization=true

# Commented because the rule has moved to the Configuration
#spring.security.user.name=jamal
#spring.security.user.password=newpass
#server.servlet.session.timeout=1m